-- 昭島市 歳出（当初予算）。原典と1対1。
--
-- ⚠️ **原典は CSV ではなく PDF の事項別明細書である。** 昭島市はオープンデータカタログを
-- 持たず、予算資料は市サイトの PDF しかない（2026-08-30 実測）。`data/budget/raw/` に
-- あるのは `ingestion/budget/extract_statement.py` が起こした表で、原文のバイト列ではない
-- （`raw_form = "extracted"`）。**復元一致の検査は成立しない** — 抽出の保証は
-- 目の見出し金額との突合・節列との突合・行のまとめ方への非依存で、証跡に書いてある。
--
-- ⚠️ **説明欄の3段がそのまま階層になる**（事業 → 節 → 細節）。法定様式の作りで、
-- 歳入とは並びが違う（歳入の説明欄は節の内訳が1段）。
-- ⚠️ **説明欄の節にはコードが無い。** コードを持つのは目の集計としての節列のほうで、
-- 名前で突き合わせるのは判断なので取り込みではやらない（core より後の仕事）。
{{ budget_staging('132071', 'expenditure') }}
