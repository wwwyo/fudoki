-- 多摩市 歳出（当初予算）。原典と1対1。
--
-- ⚠️ **三鷹市とも狛江市とも書式が違う。** 実測（2026-08-30、2年度 3,020 行）。
--   * 階層ごとに「コードの列」と「名称の列」を対で持つ（`款` と `款名称`）。
--     三鷹市の「1セルに2桁コード + 名称」とも、狛江市の「コードだけ」とも違う。
--   * 目の下の事業階層は**細目**で、名称が付いている（例: `子ども若者育成支援事業`）。
--     ⚠️ 歳入は節の下に細節が入るので、**歳出と歳入で階層の並びが違う**。
--   * 会計にコードの列が無い（`会計名称` だけ）。原典が持たないので code には名称が入る。
--   * コードのゼロ埋めが年度で変わる（R3 は `01`/`001`、R4 は `1`/`100`）。
--     staging は原文のまま持ち、揃えるかどうかは判断なので core より後の仕事。
--   * 金額は1段階だけ（`予算額`）。決算書と違い package 段での展開が要らない。
{{ budget_staging('132241', 'expenditure') }}
