-- **宣言した単位が、倍率とも原典の列名とも食い違っていないこと。**
--
-- 単位は `budget_amounts` だけが宣言する（取得側は持たない）。宣言が1つになると
-- 突き合わせる相手が消えるので、相手を原典に取る — 原典の列名が `予算現額(千円)` と
-- 自分で単位を名乗っているので、そちらと照合する。
--
-- ⚠️ **行ではなくコンパイルエラーで落ちる。** 見ているのはデータではなく宣言なので、
-- モデルを1つ組む前に止まるほうが読みやすい。ここに置いてあるのは母集団のためで、
-- `budget_amount_units()` が宣言の側から全ての (団体, direction, 段階) を列挙する。
{{ check_budget_amount_units() }}
select null as jurisdiction where false
