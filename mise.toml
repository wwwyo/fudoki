[tools]
bun = "1.3.14"
duckdb = "1.5.5"
# OCR（歳入科目名の抽出）専用。ingestion を回すときしか要らないので、
# CI は `MISE_DISABLE_TOOLS` で除外している（.github/workflows/verify.yml）。
"llama.cpp" = { version = "b9616", version_prefix = "b" }
python = "3.13.15"
uv = "0.12.5"
