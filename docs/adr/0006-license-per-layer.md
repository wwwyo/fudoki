# ライセンスは層ごとに分ける（コード=MIT、データは各層で宣言）

コードは fudoki のものだが原典と正本は各自治体のもので、持っていない著作権にライセンスは与えられない。root の MIT LICENSE を data/ まで及ぶと読ませると CC BY が求める帰属を下流が落とすので、層ごとの宣言を `data/LICENSE`・各 `datapackage.json`・団体ごとの `sources.toml` に持つ。原典の利用条件が未判断の団体では配布物に `licenses` を書かない（未確定は書かないことで表す）。
