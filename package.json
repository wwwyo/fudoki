{
  "name": "fudoki",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "bun run report && bun run --cwd web build",
    "check:budget": "bun run scripts/check-budget-granularity.ts",
    "check:bulletins": "bun run scripts/check-bulletins.ts",
    "dbt": "cd dbt && DBT_PROFILES_DIR=. uv run dbt build",
    "dev": "bun run report && bun run --cwd web dev",
    "fetch:fdp-taxonomy": "bun run scripts/fetch-fdp-taxonomy.ts",
    "fetch:robots": "bun run scripts/fetch-robots.ts",
    "pipeline": "uv run python -m ingestion.fetch 132047:2024 && bun run dbt && uv run python -m fdp.build && bun run report",
    "report": "bun run report/build.ts",
    "survey:budget-years": "uv run python -m ingestion.survey_years",
    "typecheck": "tsc --noEmit",
    "validate": "bun run scripts/validate-gates.ts"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
