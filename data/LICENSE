# data/ のライセンス

**リポジトリ直下の `LICENSE`（MIT）はコードに対するものです。ここには及びません。**

`data/` には出所の違う3種類が入っており、それぞれ誰のものかが違います。

| 何 | 誰のものか | ライセンス |
|---|---|---|
| `raw/`（`raw_form: verbatim`） | 各自治体 | 原典のライセンス。取得元ごとに `ingestion/budget/sources.toml` が持つ |
| `raw/`（`raw_form: extracted`） | — | 原文から抽出した**事実**。著作物ではないので原典のライセンスは付いてこない |
| `datapackages/<団体コード>/` | 各自治体 | 同上。**fudoki が選んだものではない** |
| `datapackages/derived/` | fudoki | CC BY 4.0 |

⚠️ **原典のライセンスをここに列挙しない。** 取得元ごとに違い、増えるたびに変わるので、
文書に写すと検査されないまま古くなります。正確な値は各 `datapackage.json` にあり、
取得元ごとの宣言は `ingestion/budget/sources.toml` が持ちます。

正確な表示は各 `datapackage.json` にあります。機械可読なほうを正本とし、この文書は要約です。

| フィールド | 意味 | FDP の標準か |
|---|---|---|
| `licenses` | どういう条件で使ってよいか | ○ |
| `sources` | 元になった資料と URL | ○ |
| `attribution` | 出典としてこう書いてほしい一文 | **✗ 独自** |
| `modified` / `modifications` | 原典に手を加えたか、何を加えたか | **✗ 独自** |
| `fudoki.rights.factsFrom` | 利用許諾の無い資料から事実を抽出した部分の出所 | **✗ 独自。該当するときだけ出る** |

⚠️ 帰属と改変の明示は CC BY の**義務**ですが、FDP の profile には置き場がありません
（実測で確認）。だから独自フィールドで持つほかなく、標準しか読まない実装からは
見落とされます。**義務の伝達を独自フィールドに頼っている**ことは弱点として残ります。

## fudoki はライセンスを付け替えていない

原典の著作権は各自治体にあり、fudoki は持っていません。
**持っていないものにライセンスを与えることはできない**ので、正本の `licenses` は
原典に付いてくる条件をそのまま表示しているだけです。

⚠️ この区別が崩れると実害が出ます。MIT は帰属表示を求めませんが、
CC BY 4.0 は帰属・ライセンス表示・改変の明示を求めます（§3(a)）。
`data/` まで MIT だと読んだ利用者が帰属を落とすと、それは CC BY 違反であり、
そう仕向けたのは fudoki だということになります。

## 改変している

CC BY 4.0 は改変したらその旨を示すことを求めます（§3(a)(1)(B)）。
fudoki は原典を改変しているので、各 `datapackage.json` の `modified` と
`modifications` に何をしたかを書いてあります。原典そのものは `raw/` に
Parquet で保全してあるので、改変の前後を突き合わせられます。

## 派生を CC BY 4.0 にしている理由と、その前提

`datapackages/derived/` は COFOG の割当と規則表で、これは fudoki 自身の判断です。
だからライセンスを選べます。CC BY 4.0 を選んだのは原典と同じ系統に揃えるためです。

⚠️ **この選択は原典が継承（ShareAlike）を求めないことに依存しています。**
CC BY-SA を採る団体が入った時点で派生も BY-SA にするほかなくなります。
`fdp/build.py` は原典が CC BY 4.0 と NOASSERTION 以外になったら停止するので、
黙って前提が崩れることはありません。

## 止めているのは「原文の複製」であって「配布物」ではない

⚠️ **ここを取り違えると、配れるはずのものまで止まります。**

`redistribute` が `allow` でない取得元（自治体サイトの PDF など）でも、
そこから**抽出した事実**（事業名・金額・コード）は配布物に入ります。
事実は著作物ではないからです — 著作権法2条1項1号は著作物を
「思想又は感情を創作的に表現したもの」と定義しており、事業名と金額はそれにあたりません。

止まるのは**原文そのものをリポジトリへ置くこと**だけです。

| | 原文を `raw/` に置く | 抽出した事実を配る |
|---|---|---|
| `redistribute = allow` | ○ | ○ |
| `redistribute = review` / `deny` | **✗** | ○ |

⚠️ **これが成り立つのは事実だけです。** PDF の事業説明や節説明のような散文を
そのまま持ってくると表現の複製になり、この整理は通りません。抽出する側が線を引く必要があります。

## `raw/` の保証は団体で違う

上の帰結として、`raw/` に何が入っているかが取得元ごとに変わります。
**宣言の正本は `ingestion/budget/sources.toml` の `raw_form`** です。

⚠️ 取得コードは証跡（`provenance.json`）にもこの値を書きますが、
**commit 済みの証跡にはまだ入っていません**（`raw_form` を足す前に取得したもので、
取得元が不安定で取り直せていない）。証跡だけを見て判断しないでください。

| `raw_form` | 中身 | 検査 |
|---|---|---|
| `verbatim` | 原文そのもの | 復号が可逆か・原文を復元できるかを検査済み |
| `extracted` | 原文から抽出した事実 | **不可逆なので復元は検査できない** |

`verbatim` は `redistribute = allow` のときしか宣言できません（宣言した時点で弾かれます）。
`extracted` の団体では「原典と1対1」は成立せず、再現性の保証は
**証跡（URL・SHA-256・取得時刻）と抽出コードから取り直せること**に変わります。

## 未定のライセンスをどこに書くか

配布物の descriptor には現れません。**未定という状態を持つのは取得元の宣言のほう**で、
`sources.toml` の `license_id` に SPDX の `NOASSERTION`（判断していない）を使います。

自治体サイトの PDF は、サイトに著作権表示が**ある**ので `NONE`
（ライセンスが見つからない）ではなく、「その表示が数表にどこまで及ぶかを
fudoki は判断していない」という意味で `NOASSERTION` が正確です。

配布物の側には、その取得元から来たのが事実だけであることと、
だから原典のライセンスが付いてこないことを `fudoki.rights` に書きます。
