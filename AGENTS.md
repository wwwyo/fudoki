# kotonoha（言の葉）

日本の地方議会の会議録を **Popolo** スキーマに正規化して、オープンデータと MCP サーバとして配布する。

名前は古今和歌集の仮名序「よろづの言の葉とぞなれりける」から。扱う対象＝議会の発言そのもの。

## なぜ作るか

予算がどう決まり何に使われたかの**過程**は会議録にしか残らない。予算書は結果、事業評価は自己申告で、「なぜその額が付いたか」は議事にしか出てこない。

その会議録は公開されているのに機械可読ではない。横断も経年比較もできず、予算科目とも事業評価とも突き合わせられない。**法律上は再利用できるのに、形式が閉じている。**

最終的に狙うのは「議事録が読める」ことではなく、**予算がどう決まり、何に使われ、効果がどうだったかを追える**ようにすること。英米の先行事例（後述）はすべて bill tracking であって budget-outcome tracking ではなく、そこは世界的に空白。

## 設計方針

**1. 独自スキーマを発明しない。**
人・組織・役職・発言は [Popolo](https://www.popoloproject.com/) に**完全準拠**させる。本文構造は [Akoma Ntoso](https://www.oasis-open.org/standard/akn-v1-0/) 互換を意識する（AKN 完全準拠は後追いでよい）。標準に載ること自体が相互運用性の主張であり、自作すると必ず既存ツールから外れる。

**2. 特定自治体向けサイトではなく汎用層として作る。**
mySociety が TheyWorkForYou（UK専用サイト）と SayIt（汎用ツール）を分離したから、南アフリカやフィラデルフィアが再利用できた。同じ分け方をする。「他自治体でも動く」を口約束ではなくコードで示す。

**3. 運営者が消えても止まらない形にする。**
先行事例は2つの死に方をした — 個人運営は消滅（chiholog は798自治体を横断検索していたが現在 DNS ごと消えている）、会社運営は無料枠の線引きを握られた（Open States は Plural に買収後、非商用のみ無料・GraphQL v2 はサンセット）。データごとリポジトリに置き、CI で更新し、MIT で fork 可能にする。

**4. 取得と正規化を分離する（ELT）。**
自治体ごとの表記揺れの吸収ルールは必ず後から直る。生データを保持して Transform をやり直せることが前提。詳細は「パイプライン」節。

## 制約

**再利用の根拠は著作権法40条1項**（公開して行われた政治上の演説・陳述は、方法を問わず利用できる）。

ただし但書に「**同一の著作者のものを編集して利用する場合を除く**」がある。したがって:

> **議員個人の発言だけを集めて編集した成果物を作らない。**

会議単位・議題単位で発言を並べるのは問題ないが、「この議員の発言集」を生成物として出力しない。話者で絞り込む検索機能と、話者別に編集した著作物は別物なので、UI の実装時も意識する。

各議会が独自に掲げる利用規約は著作権とは別レイヤーの契約であり、自治体ごとに異なる。取得元を追加するたび確認する（手順は `kaigiroku` skill）。法的な最終判断は専門家確認が要る。

## 対象

**東京都の全区市町村（62団体）を最初の網羅範囲**とする。識別子は総務省の全国地方公共団体コード（東京都のオープンデータ URL 自体がこの体系。例: 葛飾区 `131229`、狛江市 `132195`）。予算・決算データと突き合わせるときも同じキーで繋がる。

会議録システムは自治体ごとに異なるので、取得先は団体コードごとに解決する。`kaigiroku.net`（DiscussNetPremium、全国477〜492自治体）が最大勢力だが、**東京の区市は未導入が多い**（練馬区は導入済み、品川区・世田谷区・杉並区・町田市・狛江市は未導入）。

## パイプライン

生データを捨てない **ELT**。Transform のルールが変わっても再取得しない。

| 段 | 責務 | やらないこと |
|---|---|---|
| **Extract** | 会議録システムからの取得。レスポンスを**無加工**で保存 | 解釈・整形・結合 |
| **Load** | 取得物を一律の中間表現へ。1発言1レコード。表記は原文のまま | 名寄せ・表記統一 |
| **Transform** | Popolo へのマッピング。話者の名寄せ、職名↔氏名の解決、表記揺れの吸収 | 取得 |

**Extract は冪等**にする（同じ入力なら同じ出力、既存があればスキップ）。**Transform は生データだけから何度でも再生成できる**こと。この2つが崩れると、表記ルールを直すたびに全自治体を再クロールすることになる。

## 技術スタック

- Bun + TypeScript
- 配布: Popolo 準拠 JSON（リポジトリに commit）+ MCP サーバ

## セットアップ

ツールは mise で管理している。

```bash
mise install
bun install
```

## 参考にする先行事例

| | 借りるもの |
|---|---|
| [ParlParse](https://github.com/mysociety/parlparse) | UK議会のスクレイパ／パーサ。`pyscraper/` `members/` `rawdata/` の分離と、**議員マスタを独立させる**設計 |
| [SayIt](https://www.mysociety.org/democracy/sayit/) | 議事録公開の汎用ツール。全文検索・話者フィルタ・**発言単位の permalink**・SEO という機能セット |
| [Open States / Plural Open](https://open.pluralpolicy.com/) | 米50州の立法データを標準化して無料 API + bulk download で配布。Popolo ベースのスキーマ運用 |

## skills

| skill | 使う場面 |
|---|---|
| [kaigiroku](.agents/skills/kaigiroku/SKILL.md) | kaigiroku.net から会議録を取得する。API の叩き方・tenant_id・レスポンスの自治体差・規約確認 |
